import { KSafePipePipe } from './k-safe.pipe.pipe';

describe('KSafePipePipe', () => {
  it('create an instance', () => {
    const pipe = new KSafePipePipe();
    expect(pipe).toBeTruthy();
  });
});
